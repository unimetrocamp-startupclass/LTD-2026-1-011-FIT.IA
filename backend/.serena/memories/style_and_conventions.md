TypeScript ESM code with `.js` extensions in relative imports. Zod schemas live in src/schemas and are reused by routes for body/response typing. Use cases are classes under src/usecases with InputDto/OutputDto interfaces. Prisma client is imported from src/lib/db.js and generated Prisma enums from src/generated/prisma/enums.js. Keep changes focused and follow existing two-space indentation and semicolon style.